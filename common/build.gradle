apply plugin: 'kotlin-platform-common'

archivesBaseName = 'simplex-lp-solver-common'

dependencies {
    compile libraries.kotlin_stdlib.common

    testCompile libraries.kotlin_test.common_annotations
    testCompile libraries.kotlin_test.common
}